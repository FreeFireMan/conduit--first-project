

const initialState = {}

export const errorReducer = (state = initialState, action) => {
  const {type, payload} = action

  switch (type) {

    case AAAA: {
      return {...state}
    }

    case AAAA: {
      return {...state}
    }

    default: {
      return state
    }
  }
}
